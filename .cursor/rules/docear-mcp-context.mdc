---
description: Docear MCP 操作前必须先读取当前打开导图和选中节点
alwaysApply: true
---

# Docear MCP：先读当前上下文

对 Docear 思维导图的任何 MCP 读/写操作之前：

1. 调用 `get_selection_context`；只读查任意导图用 `search_nodes` / `get_mindmap_json`（**禁止**为查信息调用 `open_mindmap`）。问「现在/最近」类问题须加 `modifiedWithinDays` 或先用 `list_recently_modified`
2. 以返回的 `mapFile`、`nodeId` 为准，不默认 `test.mm`，不复用旧会话节点 ID
3. 用户说「记到这里」时，父节点 = 当前选中节点；跨文件写入须传 `filePath`（来自 `search_nodes.mapFile`）
4. 完成后在回复中说明：当前导图、选中节点、写入位置
5. **写入时分类清晰**：先分大类（工具/资源/提示词/用法），再分子类；一条节点一个主题，不挤长句
6. **批量写节点**用 `add_nodes`（支持 `children` 多层，一次保存）；禁止对同一父节点连续多次 `add_node`
7. **查关联**用 `get_relationship_graph` / `get_node_relationships`（静默）；大库默认 `map_files` 或加 `query`/`filePath` 缩小范围；也可用 `mode: tags` / `favorites`
8. **查标签/分组**用 `get_tag_catalog` / `list_tag_groups` / `list_tags` / `list_nodes_by_tag` / `list_favorites`（侧栏索引，非全库扫盘；分组支持无限嵌套）
9. **每次 MCP 调用传 `_audit`**：`caller`、`traceId`、`questionSummary`、`operationGoal`；明细写入 `%APPDATA%\\Docear\\_data\\audit.db`
10. **查番茄钟/专注**：`get_running_pomodoro` / `get_pomodoro_stats` / `get_pomodoro_history`（问「现在在干什么、什么时间干了什么」时优先）；`get_selection_context` 含 `runningPomodoro`

详见技能：`docear_plugin_mcp/cursor-plugin/skills/docear-mcp-context/SKILL.md`（发布源；Docear 启动时会自动同步到 Cursor）
